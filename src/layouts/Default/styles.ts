import styled from "styled-components";

export const LayoutContainer = styled.div`
  max-width: 90rem;
  height: calc(100vh - 10rem);

  margin: 0 auto;

  .container{
    max-width: 864px;
    margin: 0 auto;
    margin-top: -5.5rem;
    position: relative;
  }
`;
